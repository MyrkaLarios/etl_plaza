<h2>Contratante</h2>
<div style='display: flex; flex-direction: column;'>
  <% contratante.each do |e| %>
    <div style='display: flex; justify-content: space-between;' class='my-2'>
      <p><%= e.nombre %></p>
      <p><%= e.direccion %></p>
      <p><%= e.numero %></p>
      <%= render partial: "dashboard/links", locals: { e: e, table: 'dbo.CONTRATANTE' } %>
    </div>
  <% end %>
</div>