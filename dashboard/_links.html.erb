<%= link_to 'Modificar', "/modify?table=#{table}&id=#{e.id}&s=#{e.sistema}", class: 'btn btn-secondary' %>
<%= link_to 'Eliminar', "/delete?table=#{table}&id=#{e.id}", class: 'btn btn-secondary' %>
