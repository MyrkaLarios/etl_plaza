<h2>Negocios</h2>
<div style='display: flex; flex-direction: column;'>
  <% tipos_empleado.each do |e| %>
    <div style='display: flex; justify-content: space-between;' class='my-2'>
      <p><%= e.nombre %></p>
      <p><%= e.giro %></p>
      <p><%= e.estado %></p>
      <%= render partial: "dashboard/links", locals: { e: e, table: 'dbo.NEGOCIOS' } %>
    </div>
  <% end %>
</div>