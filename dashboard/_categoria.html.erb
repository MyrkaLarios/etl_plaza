<h2>Categoria</h2>
<div style='display: flex; flex-direction: column;'>
  <% categoria.each do |e| %>
    <div style='display: flex; justify-content: space-between;' class='my-2'>
      <p><%= e.nombre %></p>
      <%= render partial: "dashboard/links", locals: { e: e, table: 'dbo.CATEGORIA' } %>
    </div>
  <% end %>
</div>