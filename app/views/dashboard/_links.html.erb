<div>
  <%= link_to 'Modificar', "/modify?table=#{table}&id=#{e.id}&s=#{e.sistema}", class: 'btn btn-outline-primary' %>
  <%= link_to 'Eliminar', "/delete?table=#{table}&id=#{e.id}", class: 'btn btn-outline-danger' %>
</div>
