<div class="card-header" id="heading7">
  <h2 class="mb-0 d-flex justify-content-between">
    <button class="btn btn-link collapsed" data-toggle="collapse" data-target="#collapse7" aria-expanded="false" aria-controls="collapse7">
      + Cajero
    </button>
    <% if current_user.user_type != 1 %>
      <%= link_to 'Eliminar', "/delete_table?table=dbo.CAJERO&sistema=#{s}", method: :delete, class: 'btn btn-dark' %>
    <% end %>
  </h2>
</div>

<div id="collapse7" class="collapse" aria-labelledby="heading7" data-parent="#accordion">
  <div class="card-body">
    <div style='display: flex; justify-content: space-between;' class='my-2'>
      <p class="border-bottom" id="top">Id</p>
      <p class="border-bottom" id="top">Dinero Actual</p>
      <p class="border-bottom" id="top">Fecha Actual</p>
      <p class="border-bottom" id="top">Periodicidad Mantenimiento</p>
      <p class="border-bottom" id="top">Estado</p>
      <p class="border-bottom" id="top">Id Área</p>
      <% if current_user.user_type != 1 %>
        <div class="border-bottom" id="top" style='width: 180px; margin-bottom: 1rem'></div>
      <% end %>
    </div>

    <div style='display: flex; flex-direction: column;'>
      <% cajeros.each do |e| %>
        <div style='display: flex; justify-content: space-between;' class='my-2'>
          <p><%= e.id %></p>
          <p class="<%= 'wrong' if !valid_salary?(e.dineroactual) %>"><%= e.dineroactual %></p>
          <p class="<%= 'wrong' if !valid_date?(e.fechaactual) %>"><%= e.fechaactual %></p>
          <p class="<%= 'wrong' if !valid_periodicidad?(e.periodicidadmantenimiento) %>"><%= e.periodicidadmantenimiento %></p>
          <p class="<%= 'wrong' if !valid_estado_cajero?(e.estado) %>"><%= e.estado %></p>
          <p><%= e.id_area %></p>
          <% if current_user.user_type != 1 %>
            <%= render partial: "dashboard/links", locals: { e: e, table: 'dbo.CAJERO' } %>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>
