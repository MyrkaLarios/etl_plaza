<div class="card-header" id="heading28">
  <h2 class="mb-0">
    <button class="btn btn-link collapsed" data-toggle="collapse" data-target="#collapse28" aria-expanded="false" aria-controls="collapse28">
      + Tipo Incidente
    </button>
  </h2>
</div>

<div id="collapse28" class="collapse" aria-labelledby="heading28" data-parent="#accordion">
  <div class="card-body">
    <div style='display: flex; justify-content: space-between;' class='my-2'>
      <p class="border-bottom" id="top">Id</p>
      <p class="border-bottom" id="top">Nombre</p>
      <p class="border-bottom" id="top">Descripción</p>
      <div class="border-bottom" id="top" style='width: 180px; margin-bottom: 1rem'></div>
    </div>

    <div style='display: flex; flex-direction: column;'>
      <% tipos_incidentes.each do |e| %>
        <div style='display: flex; justify-content: space-between;' class='my-2'>
          <p><%= e.id %></p>
          <p><%= e.nombre %></p>
          <p><%= e.descripcion %></p>
          <%= render partial: "dashboard/links", locals: { e: e, table: 'dbo.TIPO_INCIDENTE' } %>
        </div>
      <% end %>
    </div>
  </div>
</div>
